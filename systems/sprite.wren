foreign class Sprite {
    construct new(file) {}

    foreign layer=(value)
    foreign setFrameSize(w, h)
    foreign frame=(value)
    foreign angle=(value)
    foreign direction=(value)

    foreign setPosition(x, y)

    foreign render()
}
