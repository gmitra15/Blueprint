class Board { 
	construct new() {
		_sections = []	
		_rows = []
		for(i in 0...21){
			_sections.add(Section.new(i))	
			if(i in 0...1){
				var s = Section.new(i)
				_sections.add(s)
				if(s in 
			}
		
		}
			
	}
	boardSwap() {}
}
